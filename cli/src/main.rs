//! `disk-tools` — the CLI frontend.
//!
//! Placeholder: Task 6 replaces this with the clap surface
//! (`args -> ScanOptions -> core::scan -> render`).

fn main() {
    let _options = disk_tools_core::ScanOptions::default();
}

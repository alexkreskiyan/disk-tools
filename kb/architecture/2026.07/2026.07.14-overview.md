---
title: "disc-tools — Architecture Overview"
description: "Scaffold snapshot for an empty Rust chat/Discord tooling repo. Structure and intent only; no implementation exists yet."
type: architecture
status: snapshot
version: "1.0.0"
created: 2026-07-14
updated: 2026-07-14
author: skill::document-repository
modified_by: skill::document-repository
---

# disc-tools — Architecture Overview

> **Scaffold snapshot.** The repository currently contains only `.gitignore` and IDE config. This document records intent inferred from `.gitignore`, to be replaced with real architecture once code exists.

## Current State

| Aspect | Status |
|--------|--------|
| Source code | none |
| Build manifest (`Cargo.toml`) | absent |
| Tracked files | `.gitignore` only |
| IDE module type | `EMPTY_MODULE` |

## Inferred Intent

Signals come solely from `.gitignore`:

| Pattern | Inference |
|---------|-----------|
| `/.cargo/`, `/target/` | **Rust** project (Cargo build artifacts) |
| `/chat/*.txt` | Consumes/produces chat exports as text files |
| `/logs/*.log` | Emits runtime logs to `logs/` |
| `.env`, `.direnv/` | Env-based config, likely via `direnv` |

Name "disc-tools" + `chat/` suggests **Discord (or chat) data tooling** — e.g. export parsing, log processing, or bot utilities. Unconfirmed.

## Intended Layout (TODO)

```
disc-tools/
├── Cargo.toml          # crate manifest — TODO
├── src/
│   ├── main.rs         # entrypoint — TODO
│   └── ...             # modules — TODO
├── chat/               # chat exports (gitignored, runtime)
└── logs/               # logs (gitignored, runtime)
```

## Design Decisions

_None recorded yet._ Capture crate structure, data flow (chat → processing → output), and error/logging strategy here as they are decided.

## Open Questions

- Binary CLI, library, or Discord bot service?
- What consumes `chat/*.txt` — parser input or tool output?
- Async runtime (tokio) needed? (likely, if it talks to Discord)
- Single crate or workspace?

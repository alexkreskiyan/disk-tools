---
title: "disc-tools — Development Guide"
description: "Scaffold dev guide for the empty disc-tools Rust repo: how to bootstrap the crate and keep docs in sync."
type: guide
status: snapshot
version: "1.0.0"
created: 2026-07-14
updated: 2026-07-14
author: skill::document-repository
modified_by: skill::document-repository
---

# disc-tools — Development Guide

> **Scaffold snapshot.** No build system exists yet. This guide covers bootstrapping and the doc workflow; replace command placeholders once `Cargo.toml` lands.

## Bootstrapping the Crate

The repo is empty. To start:

```bash
cargo init                 # binary crate, or:
cargo init --lib           # library crate
```

`.gitignore` already excludes `/target/` and `/.cargo/`, so Cargo output stays untracked.

## Commands (TODO)

Fill in once the crate exists:

| Command | Description |
|---------|-------------|
| `cargo build` | Compile |
| `cargo run` | Run |
| `cargo test` | Run tests |
| `cargo clippy` | Lint |
| `cargo fmt` | Format |

## Environment

- `.env` and `.direnv/` are gitignored → use `direnv` / a `.env` for local config.
- Add a committed `.env.example` documenting required vars (never commit real secrets).
- Runtime output dirs `chat/` and `logs/` are gitignored; create them as needed.

## Testing

_TODO_ — no tests yet. Document the framework (built-in `#[test]`, `cargo test`) and integration test layout (`tests/`) once added.

## Documentation Workflow

1. Any code change → update the matching snapshot in `kb/architecture/` or `kb/guides/`.
2. Structural changes → update `CLAUDE.md` (Project Structure, Quick Reference).
3. After a meaningful milestone, re-run `/document-repository` to write a fresh dated snapshot.
4. Keep `CLAUDE.md` Documentation links pointed at the newest snapshots.

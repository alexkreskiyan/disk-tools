---
title: "Feature Spec: disk-tools v0.2 — detectors + cleanup engine"
description: "Junk-pattern and old-file detectors as a post-aggregate pass over ScanTree, plus a `disk-tools clean` subcommand: dry-run by default, OS trash on apply, safe-list categories with built-in defaults, a never-touch denylist and a git-dirty guard."
type: spec
status: draft
version: "1.0.0"
created: 2026-07-25
updated: 2026-07-25
author: skill::write-spec
modified_by: skill::review-spec
concept: "kb/concepts/2026.07/2026.07.14-disk-tools.md"
---

# Feature Spec: disk-tools v0.2 — detectors + cleanup engine

## 1. Overview

v0.1 answers *what is eating my disk*. v0.2 answers the question the project was
started for: **so you can clean it**. It adds two detectors (junk patterns, old
files) and a cleanup engine behind a new `disk-tools clean` subcommand — dry-run by
default, OS trash on apply, and four independent safety mechanisms.

This is the first version that **deletes files**, which sets the tone for every
decision below: the planning half is a pure function with no filesystem writes, all
failures are data, and the documentation of the safety model is a task in its own
right.

- **Concept:** [2026.07.14-disk-tools.md](../../concepts/2026.07/2026.07.14-disk-tools.md) (§3, §4; Roadmap v0.2)
- **Predecessor:** [v0.1 spec](2026.07.14-disk-tools-v0.1-scan-report.md) — all 11 tasks Done
- **Related plan:** [walk parallelism](../../plans/2026.07/2026.07.25-walk-parallelism.md) — deliberately after v0.2, since detectors touch the same loop

## 2. Approach

### Architecture

```
                    disk-tools (cli)                         disk-tools-core
                    ────────────────                         ───────────────
  disk-tools <PATH>            ──ScanOptions──────────────►  scan()  → ScanTree
  disk-tools clean <PATH> …    ──ScanOptions──────────────►  scan()  → ScanTree
           │                                                        │
           │                   ──CleanOptions─────────────►  clean::plan(&ScanTree, &CleanOptions)
           │                                                        │
           │                                                        │  detect::junk()   siblings via children
           │                                                        │  detect::old()    mtime threshold
           │                                                        │  denylist  → drop
           │                                                        │  tiers     → auto | confirm
           │                                                        │  git guard → drop with reason
           │                                                        ▼
           │                                                    CleanPlan
           │                                          { candidates, reclaimable, excluded }
           ├─ render/clean.rs  ◄─────────────────────────────────────┤   dry-run: flat list + total
           │                                                         │
           └─ --apply  ────────────────────────────────►  clean::apply(&CleanPlan) → CleanOutcome
                                                             trash; per-item failures as data
```

The split that matters: **`plan()` is pure** — it reads a `ScanTree` and produces a
decision, touching nothing. Every rule, every exclusion and every byte of the
reclaimable total is therefore testable without a filesystem that can be damaged.
`apply()` is the only code in the project that removes anything.

### Design decisions

| # | Decision | Chosen approach | Rationale | Alternatives considered |
|---|----------|-----------------|-----------|------------------------|
| D1 | Where detection runs | **Post-aggregate pass over the built `ScanTree`** | `ScanNode.children` already groups siblings, which is exactly what marker-aware rules need (`target/` beside `Cargo.toml`); the hot parallel walk is untouched; the concept explicitly says not to build the `Detector` abstraction early | A `Detector` trait threaded through `walk_dir` — invasive, and the walk has no sibling view to give it |
| D2 | Age signal | **mtime**, via `modified: Option<SystemTime>` on `WalkEntry` and `ScanNode` | `entry.metadata()` is already called per entry (`walk.rs:170`), so this costs **no extra syscall**; on Windows `LastWriteTime` already arrives in the directory listing. atime is rejected: `relatime`/`noatime` are common defaults, which makes it least trustworthy on exactly the caches this rule targets | atime (unreliable); an `--age-by` flag offering both (surface and tests for a rare case) |
| D3 | CLI grammar | clap's default-subcommand pattern: `args_conflicts_with_subcommands`, flattened scan args, `Option<Command>` | `disk-tools <PATH>` keeps working verbatim, which the v0.1 spec's forward-compat note anticipated | Manual first-token sniffing (fragile, breaks `--help`); migrating to `disk-tools scan <PATH>` (breaks the existing surface for no gain yet) |
| D4 | Engine location | `core::clean`, split pure `plan()` / effectful `apply()` | Detectors belong in core; the split makes deletion decisions testable without a filesystem | Cleanup in the CLI — untestable and splits the rules from the model |
| D5 | Failure handling | Per-item outcomes as data, mirroring `SkippedEntry` | The core's standing rule is "never fails, returns what went wrong as data"; a half-completed trash must be reported, never swallowed | `Result` per item — contradicts the existing model and loses the partial-success detail |
| D6 | git-dirty guard | Shell out to `git status --porcelain`; repo present but `git` missing ⇒ treat as dirty | Avoids a heavy dependency for one boolean (`gix` is large, `git2` needs libgit2); conservative when it cannot know | Vendoring a git implementation; skipping the guard when git is absent (unsafe default) |
| D7 | Reclaimable size | Sum of attributed `allocated`, with multi-link candidates **marked** and the total stated as an upper bound | Only one of three hardlink cases over-reports (§8.3), and `nlink` detects it for free from metadata already fetched — the same free ride as `modified`. Marking beats both a silent wrong number and an expensive exact one | A README-only caveat (user cannot see *which* number is soft); a full link census (expensive, a project of its own) |
| D8 | Safe-list defaults | Built into the binary; no config required to clean | Concept §4: "Categories ship with built-in defaults in v0.2"; config (v0.3) only toggles them | Requiring config first — would make v0.2 unusable standalone |
| D9 | Age rule activation | **Opt-in**: `--older-than <DURATION>`, no default; absent flag means the rule does not run | An always-on age rule would flood the first dry run with confirm-tier entries and bury the safe-list candidates that are the point of the feature. `--safe` ignores them anyway, so a default buys nothing | A 90-day default (noisy); dropping old-file detection from v0.2 (loses a nearly-free capability) |
| D10 | Link count portability | `links: Option<u32>` — `Some` on Unix, **`None` on Windows** | `FILE_ID_BOTH_DIR_INFO` carries no link count, so Windows would need a handle per file — the exact cost `windows_dir` exists to avoid. Follows the established platform-pair convention: implement where cheap, document the honest no-op elsewhere | A handle per file on Windows (defeats the v0.1 design); omitting the marker everywhere (loses it on the platform where hardlinks are common) |

### Components

**New:**
- `core/src/detect.rs` — junk-pattern and old-file rules over `ScanTree`
- `core/src/clean.rs` — `CleanOptions`, `CleanPlan`, `plan()`, `apply()`, `CleanOutcome`
- `cli/src/render/clean.rs` — the dry-run report and the apply summary

**Modified:**
- `core/src/walk.rs`, `core/src/tree.rs` — carry `modified` through `WalkEntry` → `ScanNode`
- `core/src/lib.rs` — re-export the new public types
- `core/Cargo.toml` — `trash` dependency
- `cli/src/args.rs` — subcommand grammar
- `cli/src/main.rs` — branch on subcommand
- `README.md`, `kb/` snapshots — the safety model

## 3. Tasks

### Task 1: Trash backend + macOS smoke test

- **Goal:** A thin wrapper over the `trash` crate, and a definitive answer to whether it is usable here at all.
- **Acceptance Criteria:**
  - [ ] Given a file in a temp dir, when trashed, then the original path no longer exists and the call reports success
  - [ ] Given a path that cannot be trashed, when trashed, then the failure comes back as data — never a panic, never a process exit
  - [ ] Given an environment with no desktop trash (CI), when the smoke test runs, then it skips loudly rather than failing
  - [ ] Given a 10,000-file tree, when trashed, then the wall-clock cost is recorded in `kb/`
- **Tests first (TDD):**
  - [ ] `trashing_a_file_removes_the_original` (`#[ignore]` — needs a real trash)
  - [ ] `trash_failure_is_reported_as_data`
  - [ ] `missing_trash_backend_skips_loudly`
- **Key files:** `core/src/clean.rs`, `core/Cargo.toml`, `kb/benchmarks/`
- **Dependencies:** None
- **Status:** Pending

#### Implementation Notes
- **First on purpose.** The concept records a downstream report of a macOS build issue with the `trash` crate and says to smoke-test before relying on it. Every apply path rests on this; if it fails, the whole approach to deletion needs rethinking, and that must surface now rather than at Task 7.
- Wrapper shape: `fn to_trash(path: &Path) -> Result<(), TrashFailure>` where `TrashFailure` carries the path and a reason string — the shape `CleanOutcome` will collect.
- The 10k-file timing answers the concept's warning that trashing a large tree "can be slow or fail on some volumes".

---

### Task 2: `modified` and link count through the model

- **Goal:** Every entry carries its mtime and, where the platform offers it, its hardlink count — both at zero syscall cost.
- **Acceptance Criteria:**
  - [ ] Given a file with a known mtime, when scanned, then `ScanNode.modified` equals it
  - [ ] Given `--json`, when emitted, then `modified` and `links` appear and an existing consumer still parses the payload
  - [ ] Given a Windows scan, when entries come from the directory listing, then `modified` is taken from `LastWriteTime` with no additional call
  - [ ] Given an entry whose metadata carries no usable timestamp, when scanned, then `modified` is `None` rather than an error
  - [ ] Given a file with two hardlinks on Unix, when scanned, then `ScanNode.links` is `Some(2)` on both paths
  - [ ] Given a Windows scan, when entries are built, then `links` is `None` — no per-file handle is opened to obtain it
- **Tests first (TDD):**
  - [ ] `modified_matches_the_files_mtime`
  - [ ] `json_carries_modified_and_links`
  - [ ] `windows_modified_comes_from_the_listing` (`#[cfg(windows)]`)
  - [ ] `missing_timestamp_yields_none`
  - [ ] `hardlinked_file_reports_two_links` (`#[cfg(unix)]`)
  - [ ] `windows_reports_no_link_count` (`#[cfg(windows)]`)
- **Key files:** `core/src/walk.rs`, `core/src/tree.rs`, `core/src/windows_dir.rs`, `cli/src/render/json.rs`
- **Dependencies:** None
- **Status:** Pending

#### Implementation Notes
- `entry.metadata()` is already called at `core/src/walk.rs:170`, so both `modified()` and Unix's `nlink()` are free. Do **not** add a second stat, and do **not** open a handle per file on Windows to chase the link count.
- `EntryFacts` (`walk.rs`) already exists as the Windows carrier and needs one more field; `FILE_ID_BOTH_DIR_INFO.LastWriteTime` is already read in `windows_dir.rs` and currently discarded.
- **`links` is `None` on Windows and that is deliberate** (D10). `FILE_ID_BOTH_DIR_INFO` carries no link count. The consequence — the Task 4 shared-content marker does not appear on Windows — must be stated in the handoff and in Task 8's documentation, not discovered later.
- `ScanNode` is public and serde-derived. Adding two fields is backward-compatible for JSON consumers, but it **is** a public API change — note it in the handoff.
- A directory's `modified` is its own, never derived from children (see Task 3).

---

### Task 3: Detectors — junk patterns and old files

- **Goal:** `core/src/detect.rs`: the four safe-list categories plus an age rule, as pure functions over the built tree.
- **Acceptance Criteria:**
  - [ ] Given `target/` with a sibling `Cargo.toml`, when detected, then it matches `rust-target`
  - [ ] Given `target/` **without** a sibling `Cargo.toml`, when detected, then it does not match
  - [ ] Given `node_modules/`, when detected, then it matches with no marker required
  - [ ] Given `__pycache__/` and a loose `*.pyc`, when detected, then both match `pycache`
  - [ ] Given a cache directory outside the user's home, when detected, then it does not match `user-caches`
  - [ ] Given a matched directory, when detected, then its children are not separately reported
  - [ ] Given no `--older-than`, when detected, then **no `old` candidates are produced at all**
  - [ ] Given a file older than the age threshold, when detected, then it matches `old`
  - [ ] Given a file exactly at the threshold, when detected, then the boundary is inclusive
  - [ ] Given a file whose `modified` is `None`, when the age rule runs, then it never matches
  - [ ] Given a directory, when the age rule runs, then it is judged by its own mtime, not its children's
- **Tests first (TDD):**
  - [ ] `rust_target_requires_a_sibling_manifest`, `bare_target_directory_is_not_junk`
  - [ ] `node_modules_matches_without_a_marker`
  - [ ] `pycache_matches_directory_and_loose_pyc`
  - [ ] `user_caches_are_scoped_to_home`
  - [ ] `children_of_a_match_are_not_reported_separately`
  - [ ] `age_rule_is_off_without_the_flag`
  - [ ] `old_file_past_threshold_matches`, `threshold_boundary_is_inclusive`, `directory_age_is_its_own`
  - [ ] `unknown_timestamp_never_matches`
- **Key files:** `core/src/detect.rs`, `core/src/lib.rs`
- **Dependencies:** Requires Task 2
- **Status:** Pending

#### Implementation Notes
- **Marker awareness comes free from the tree.** A rule receives a `&ScanNode` and its parent's `children`, so "is there a `Cargo.toml` beside this `target/`?" is a lookup in a slice the aggregation already built. No walk changes.
- **Do not descend into a match.** Once `node_modules/` matches, its subtree is one candidate — reporting 40,000 children would be both useless and dangerous.
- Categories per concept §4: `rust-target`, `node-modules`, `pycache`, `user-caches`. `user-caches` matches `~/Library/Caches`, `~/.cache`, `%LOCALAPPDATA%\Temp` — **user-scoped only**, never their system counterparts (that is the denylist's job in Task 4, but the rule itself must not match them either).
- See §8.2.1 and §8.2.2 for the rule algorithms.
- **The age rule is opt-in** (D9): with `older_than == None` it produces nothing. That is why its first acceptance criterion is the *absence* of output — the default path must be provably quiet.

---

### Task 4: Plan — denylist, tiers, reclaimable size

- **Goal:** `clean::plan(&ScanTree, &CleanOptions) -> CleanPlan`, a pure function that writes nothing.
- **Acceptance Criteria:**
  - [ ] Given `/System`, `/Library/Caches` (no tilde), `~/Library/Application Support` or `%APPDATA%`, when planning, then they are never candidates whatever else matched
  - [ ] Given a `venv/` or `.venv/`, when planning, then it is confirm-tier, never auto
  - [ ] Given a safe-list category match, when planning, then it is auto-tier
  - [ ] Given an old-file match, when planning, then it is confirm-tier
  - [ ] Given `--safe`, when planning, then only auto-tier candidates are included
  - [ ] Given a plan, when totalled, then reclaimable equals the sum of attributed `allocated` over its candidates
  - [ ] Given a candidate containing a file with `links > 1`, when planned, then the candidate is marked `shared` and the total is reported as an upper bound
  - [ ] Given a candidate with no multi-link content, when planned, then it is not marked and the total is exact
  - [ ] Given `links == None` (Windows), when planned, then nothing is marked and the total carries no upper-bound claim
  - [ ] Given the same tree twice, when planned, then the two plans are identical including ordering
  - [ ] Given a tree where nothing matches, when planned, then the plan is empty and reclaimable is 0
  - [ ] Given `plan()` on any input, when it returns, then no filesystem write has occurred
- **Tests first (TDD):**
  - [ ] `denylisted_paths_are_never_candidates` (one case per denylist entry)
  - [ ] `virtualenvs_are_confirm_tier`
  - [ ] `safe_flag_admits_only_auto_tier`
  - [ ] `reclaimable_is_the_sum_of_candidate_allocated`
  - [ ] `multi_link_content_marks_the_candidate_shared`
  - [ ] `unshared_candidate_is_not_marked`
  - [ ] `plan_is_deterministic_across_runs`
  - [ ] `empty_tree_yields_empty_plan`
- **Key files:** `core/src/clean.rs`, `core/src/lib.rs`
- **Dependencies:** Requires Task 3
- **Status:** Pending

#### Implementation Notes
- **Denylist beats every rule.** It is applied last and unconditionally; no flag overrides it. `--allow-dirty` relaxes the git guard only (Task 5), never this.
- Note the tilde distinction from the concept: `~/Library/Caches` is a **candidate**, `/Library/Caches` is **denied**. Same for the Windows pair.
- Ordering must be deterministic — sort candidates by path, as `tree::aggregate` already does for children, since the walk order is nondeterministic.
- **The `shared` marker is a "may", not a "does".** `links > 1` proves another link exists somewhere; it does not prove that link lies outside the candidate. Marking is therefore conservative by design: it says *this number is an upper bound*, never *these bytes will not be freed*. §8.3 has the case analysis.
- The marker is absent on Windows because `links` is `None` there (D10) — not because the content is unshared. The report must not imply otherwise.

---

### Task 5: git-dirty guard

- **Goal:** Refuse to clean build output of a project that has uncommitted work.
- **Acceptance Criteria:**
  - [ ] Given a build directory whose repository has uncommitted changes, when planning, then it is excluded and the plan states the reason
  - [ ] Given a clean repository, when planning, then the build directory is included
  - [ ] Given `--allow-dirty`, when planning, then the guard excludes nothing
  - [ ] Given a candidate in no repository at all, when planning, then the guard does not apply
  - [ ] Given a repository but no `git` binary on `PATH`, when planning, then the candidate is treated as dirty
- **Tests first (TDD):**
  - [ ] `dirty_repo_excludes_the_build_directory_with_a_reason`
  - [ ] `clean_repo_keeps_the_candidate`
  - [ ] `allow_dirty_disables_the_guard`
  - [ ] `path_outside_any_repo_is_unaffected`
  - [ ] `repo_without_git_binary_is_treated_as_dirty`
- **Key files:** `core/src/clean.rs`
- **Dependencies:** Requires Task 4
- **Status:** Pending

#### Implementation Notes
- `git status --porcelain` in the candidate's owning repository; non-empty output means dirty. Find the repository by walking up for a `.git` entry — do **not** call `canonicalize()` (project-wide constraint).
- The guard applies to build-output categories (`rust-target`), where "mid-work" is meaningful. It is not meaningful for `user-caches`; see §8.2.3 for which categories it gates.
- Treating a repository with no `git` binary as dirty is the conservative arm of D6: when the tool cannot know, it does not delete.
- Exclusions are **recorded in the plan with a reason**, not silently dropped — the user needs to know why something they expected is absent.

---

### Task 6: CLI — `clean` subcommand and dry-run report

- **Goal:** The new grammar, and the report that makes a dry run worth reading.
- **Acceptance Criteria:**
  - [ ] Given `disk-tools <PATH>`, when run, then the behaviour is identical to v0.1
  - [ ] Given `disk-tools clean <PATH>`, when run, then it parses with `--safe`, `--apply`, `--allow-dirty` and `--older-than`
  - [ ] Given `--older-than 90d`, when parsed, then it yields a 90-day duration; given `--older-than 12x`, then it is a clean usage error, not a panic
  - [ ] Given no `--older-than`, when parsed, then `CleanOptions.older_than` is `None`
  - [ ] Given `--help`, when printed, then both the bare form and `clean` are documented
  - [ ] Given `disk-tools clean` with no path, when run, then it is a usage error exiting with code 2
  - [ ] Given a plan, when rendered, then each candidate shows its path, category and tier, followed by a reclaimable total
  - [ ] Given a candidate marked `shared`, when rendered, then it is visibly flagged and the total is labelled an upper bound
  - [ ] Given a dry run, when it completes, then no filesystem write has occurred
  - [ ] Given an empty plan, when rendered, then it says so plainly and exits 0
  - [ ] Given a piped run, when rendered, then the report is on stdout and progress on stderr
- **Tests first (TDD):**
  - [ ] `bare_path_still_scans_exactly_as_before` (integration)
  - [ ] `clean_subcommand_parses_its_flags`
  - [ ] `older_than_parses_duration_suffixes`, `older_than_rejects_garbage`, `absent_older_than_is_none`
  - [ ] `clean_without_a_path_exits_two` (integration)
  - [ ] `dry_run_lists_candidates_with_a_total`
  - [ ] `shared_candidate_is_flagged_and_total_labelled_upper_bound`
  - [ ] `dry_run_writes_nothing` (integration, real fixture)
  - [ ] `empty_plan_renders_a_plain_message`
- **Key files:** `cli/src/args.rs`, `cli/src/main.rs`, `cli/src/render/clean.rs`, `cli/src/render/mod.rs`, `cli/tests/cli.rs`
- **Dependencies:** Requires Task 4
- **Status:** Pending

#### Implementation Notes
- Grammar per D3. The v0.1 spec's §2 forward-compat note anticipated exactly this moment and chose "bare path stays the default" over migrating to `disk-tools scan`.
- `args.rs` currently has 16 tests built on `Args::try_parse_from`; they must keep passing unchanged — that is the regression proof for the first criterion.
- The report is shaped like `render_skipped` (flat list plus a summary), **not** `render_tree`. Reuse `format_size`; do not reuse the bar/percent layout.
- `dry_run_writes_nothing` should assert against a fixture snapshot taken before and after — the strongest available statement that the default is safe.
- **`--older-than` needs a duration parser**, and `parse_size` (`cli/src/args.rs:70`) is the template: a clap `value_parser` returning `Result<T, String>` so a bad value is a usage error rather than a panic, case-insensitive suffixes, and `checked_*` arithmetic against overflow. Suffixes `d`/`w`/`m`/`y`; a bare number is rejected rather than guessed at, since "90" could plausibly mean days or seconds.
- The `shared` marker must read as *"this total may be smaller in practice"*, not as *"this will not be freed"* — see Task 4's notes for why the distinction matters.

---

### Task 7: Apply — trash execution with per-item outcomes

- **Goal:** The only task in the project that deletes anything.
- **Acceptance Criteria:**
  - [ ] Given no `--apply`, when run against a real fixture, then nothing has been trashed
  - [ ] Given `--apply`, when run, then each candidate moves to the trash and its original path is gone
  - [ ] Given one candidate that fails, when applied, then the remaining candidates still proceed and the failure is reported per item
  - [ ] Given a partial failure, when it completes, then the exit code is non-zero and the summary names what survived
  - [ ] Given a large tree, when applied, then progress is shown on stderr
- **Tests first (TDD):**
  - [ ] `without_apply_the_fixture_is_untouched` (integration)
  - [ ] `apply_moves_candidates_to_trash` (`#[ignore]` — needs a real trash)
  - [ ] `one_failure_does_not_stop_the_rest`
  - [ ] `partial_failure_exits_non_zero_and_names_survivors`
- **Key files:** `core/src/clean.rs`, `cli/src/main.rs`, `cli/src/render/clean.rs`, `cli/tests/cli.rs`
- **Dependencies:** Requires Tasks 1, 4, 6
- **Status:** Pending

#### Implementation Notes
- Per the concept: **no partial silent delete.** If trashing a tree fails midway, the outcome must say what moved and what did not.
- `apply()` returns `CleanOutcome` (§8.1) — a vector of per-candidate results. It never returns `Result` for the operation as a whole, consistent with D5.
- Deletion tests use `tempfile` fixtures exclusively and skip loudly when the environment has no trash, following the established `chmod 000` fixture pattern.

---

### Task 8: Documentation of the safety model

- **Goal:** For a feature that deletes files, the documentation is itself a safety mechanism.
- **Acceptance Criteria:**
  - [ ] Given the README, when read, then every category, both tiers, the denylist and the trash guarantee are documented
  - [ ] Given the README, when read, then the reclaimable-size hardlink caveat is stated
  - [ ] Given the README, when read, then it is unambiguous that dry-run is the default and `--apply` is required
  - [ ] Given the `kb/` snapshots, when refreshed, then the plan/apply split and the failure-as-data model are recorded
- **Tests first (TDD):** none — documentation. Verified by review against the implemented behaviour, as in v0.1's Task 10.
- **Key files:** `README.md`, `CLAUDE.md`, `kb/architecture/`, `kb/guides/`
- **Dependencies:** Requires Tasks 1–7
- **Status:** Pending

#### Implementation Notes
- v0.1's equivalent task caught three factual errors in already-written documentation. Budget for the same here: check each documented claim against the code, not against this spec.

## 4. Boundaries

### In Scope
- Junk-pattern detection for four safe-list categories with built-in defaults
- Old-file detection by age threshold
- `disk-tools clean <PATH> [--safe] [--apply] [--allow-dirty]`
- Dry-run report with reclaimable total; apply through the OS trash
- Never-touch denylist, two-tier eligibility, git-dirty guard
- `modified` on the public scan model

### Out of Scope
- **Candidate source (b) from concept §4** — "entries a user selects in the report/TUI". Needs the TUI (v0.4). v0.2 covers sources (a) detectors and (c) safe-list.
- Config file and category toggles (v0.3)
- Duplicate detection (v0.5)
- `--json` for `clean` — no consumer yet; scan's JSON is unaffected
- **Exact** reclaimable size. v0.2 reports an upper bound and marks the candidates whose figure is soft (D7); distinguishing "shared inside the candidate" from "shared with the world" needs the full link census the concept defers
- Walk parallelism work ([its own plan](../../plans/2026.07/2026.07.25-walk-parallelism.md), deliberately after v0.2)

### Protected Elements (DO NOT CHANGE)
- **`disk-tools <PATH>` must behave exactly as v0.1.** Its 16 `args.rs` tests and 16 `cli/tests/cli.rs` tests are the contract.
- **`disk-tools-core` never logs or prints.** Detectors and the cleanup engine are core code and stay silent; all prose is the CLI's.
- **`scan()` never fails.** The new `plan()` and `apply()` follow the same rule — outcomes as data.
- **`#![deny(unsafe_code)]`** with per-function exemptions only, all four currently `#[cfg(windows)]` and listed in `core/src/lib.rs`.
- **Never call `canonicalize()`** — including when walking up to find `.git`.
- Display filters (`-n`, `--depth`, `--min-size`) never affect totals.

### Constraints
- Dry-run is the default; nothing is removed without `--apply`
- Deletions go to the OS trash, never `rm`
- The denylist is absolute — no flag overrides it
- Safe-list categories work without any config file
- A partial failure is reported, never silent

## 5. Codebase Context

### Project Structure

```
core/src/
├── lib.rs           scan(); re-exports; the deny(unsafe_code) policy
├── options.rs       ScanOptions
├── walk.rs          parallel walk; WalkEntry, EntryFacts, FileId
├── size.rs          allocated + apparent (Windows: fallback only)
├── dedup.rs         hardlink attribution
├── tree.rs          ScanNode / ScanTree / SkippedEntry + aggregation
├── windows_dir.rs   cfg(windows): AllocationSize, file id, LastWriteTime
├── detect.rs        NEW — junk + old rules
└── clean.rs         NEW — CleanOptions, plan(), apply()
cli/src/
├── main.rs          args → scan → render → write
├── args.rs          clap derive; parse_size; validate_root
└── render/{tree,json,skipped}.rs  +  clean.rs NEW
cli/tests/cli.rs     black-box tests against the built binary
```

### Conventions
- Unit tests in-module (`#[cfg(test)] mod tests`); `cli/tests/cli.rs` only for process-boundary behaviour
- Platform pairs: `#[cfg(unix)]` / `#[cfg(windows)]` bodies of the same function, with the non-implementing arm documented as an honest no-op
- Failures as data (`SkippedEntry`), not `Result`, from core
- Defensive fixtures: a permission test that cannot actually restrict must skip loudly rather than pass vacuously
- `#[ignore = "reason"]` for anything needing privileges, a real mount, or a desktop trash
- The justfile is the single entry point; new operations become recipes
- KB layout `kb/<folder>/<YYYY.MM>/<YYYY.MM.DD>-<slug>.md`

### Build & Test Commands

| Recipe | Runs |
|--------|------|
| `just build` | `cargo build --workspace` |
| `just test` | `cargo test --workspace` |
| `just lint` | `cargo clippy --workspace --all-targets -- -D warnings` |
| `just check` | `cargo check --workspace --all-targets` (CI runs it pinned to MSRV 1.85) |
| `just verify` | `fmt-check` + `lint` + `test` — the pre-commit gate |
| `cargo test -p disk-tools-core -- --ignored` | the privileged/environment-dependent tests |
| `cargo clippy --workspace --target x86_64-pc-windows-msvc --all-targets -- -D warnings` | Windows type-check from a Unix host |

CI runs `just verify` + `just build` on Linux, macOS and Windows, plus an
MSRV-pinned `just check`.

## 6. Testing Strategy

**TDD workflow:** each task is implemented test-first — write a failing test for an
acceptance criterion (red), make it pass simply (green), refactor. No production
code without a failing test driving it.

For this feature there is a second reason: **`plan()` is pure**, so every safety
rule can be driven by tests over hand-built `ScanTree` values with no filesystem
involved. A denylist bug should be caught by a unit test, not by a lost directory.

### Acceptance Criteria → Tests

| Task | Criterion (abbrev.) | Test | Type |
|------|--------------------|------|------|
| 1 | trashing removes the original | `trashing_a_file_removes_the_original` | unit (`#[ignore]`) |
| 1 | failure is data | `trash_failure_is_reported_as_data` | unit |
| 2 | mtime carried | `modified_matches_the_files_mtime` | unit |
| 2 | JSON carries both | `json_carries_modified_and_links` | integration |
| 2 | Windows from listing | `windows_modified_comes_from_the_listing` | unit (windows) |
| 2 | link count on Unix | `hardlinked_file_reports_two_links` | unit (unix) |
| 2 | no link count on Windows | `windows_reports_no_link_count` | unit (windows) |
| 3 | marker required | `rust_target_requires_a_sibling_manifest` | unit |
| 3 | no marker, no match | `bare_target_directory_is_not_junk` | unit |
| 3 | caches are home-scoped | `user_caches_are_scoped_to_home` | unit |
| 3 | no child spam | `children_of_a_match_are_not_reported_separately` | unit |
| 3 | age rule is opt-in | `age_rule_is_off_without_the_flag` | unit |
| 3 | age boundary | `threshold_boundary_is_inclusive` | unit |
| 4 | denylist absolute | `denylisted_paths_are_never_candidates` | unit |
| 4 | shared marking | `multi_link_content_marks_the_candidate_shared` | unit |
| 4 | venv is confirm | `virtualenvs_are_confirm_tier` | unit |
| 4 | `--safe` filters | `safe_flag_admits_only_auto_tier` | unit |
| 4 | reclaimable total | `reclaimable_is_the_sum_of_candidate_allocated` | unit |
| 4 | determinism | `plan_is_deterministic_across_runs` | unit |
| 5 | dirty excludes | `dirty_repo_excludes_the_build_directory_with_a_reason` | unit |
| 5 | override works | `allow_dirty_disables_the_guard` | unit |
| 5 | no git ⇒ dirty | `repo_without_git_binary_is_treated_as_dirty` | unit |
| 6 | duration parsing | `older_than_parses_duration_suffixes`, `older_than_rejects_garbage` | unit |
| 6 | v0.1 unchanged | `bare_path_still_scans_exactly_as_before` | integration |
| 6 | dry run is inert | `dry_run_writes_nothing` | integration |
| 6 | usage error | `clean_without_a_path_exits_two` | integration |
| 7 | default is safe | `without_apply_the_fixture_is_untouched` | integration |
| 7 | apply trashes | `apply_moves_candidates_to_trash` | integration (`#[ignore]`) |
| 7 | partial failure | `partial_failure_exits_non_zero_and_names_survivors` | unit |

### Per-Task Testing

| Task | What to test | Type |
|------|--------------|------|
| 1 | Trash wrapper behaviour and failure shape | unit + `#[ignore]` smoke |
| 2 | mtime and link-count plumbing across platforms and JSON | unit + integration |
| 3 | Every rule, positive and negative, plus the opt-in default | unit |
| 4 | Denylist, tiers, totals, shared marking, determinism, purity | unit |
| 5 | Guard states including the unknown case | unit |
| 6 | Grammar, v0.1 regression, report shape, inertness | unit + integration |
| 7 | Deletion, partial failure, exit codes | integration + `#[ignore]` |
| 8 | Documentation reviewed against behaviour | review |

### Test Patterns
- Hand-built `ScanTree` fixtures for all `plan()` tests — no filesystem
- `tempfile::tempdir()` for anything that must touch disk; never a real user path
- Anything exercising real deletion is `#[ignore]` and skips loudly when the
  environment has no trash, following the `chmod 000` fixture precedent
- Platform gating as established; Windows symlink tests remain deliberately absent

## 7. Open Questions

**None. Implementation may start.**

*Resolved 2026-07-25.* All three were concept gaps rather than spec omissions; each
is now a design decision in §2 with its consequences threaded through the tasks:

| Was open | Resolved as | Recorded in |
|----------|-------------|-------------|
| atime or mtime for the age rule | **mtime** — atime is least trustworthy on exactly the caches the rule targets, since `relatime`/`noatime` are common defaults | D2, §8.2.2 |
| Default age and flag name | **Opt-in `--older-than <DURATION>`, no default** — an always-on rule would bury the safe-list candidates under confirm-tier noise on the very first run | D9, Task 3, Task 6, §8.2.2, §8.3 |
| Whether to mark hardlinked content | **Mark it** via `links`, which rides free on metadata already fetched; the total is then stated as an upper bound | D7, D10, Task 2, Task 4, Task 6, §8.3 |

The third answer changed the shape of the feature rather than merely settling a
detail: `ScanNode` gains a `links` field, and because `FILE_ID_BOTH_DIR_INFO`
carries no link count, the marker is **Unix-only** (D10) — a platform gap that must
be documented in Task 8, not discovered by a user comparing two machines.

## 8. Implementation Reference

### 8.1 Data Structures

#### CleanOptions

| Field | Type | Description |
|-------|------|-------------|
| `safe_only` | `bool` | `--safe`: admit auto-tier candidates only |
| `allow_dirty` | `bool` | `--allow-dirty`: disable the git guard |
| `older_than` | `Option<Duration>` | Age threshold. **`None` means the age rule does not run at all** (D9) — not "zero", not "everything" |
| `categories` | `CategorySet` | Which safe-list categories are enabled (all, by default, in v0.2) |

#### ScanNode additions (Task 2)

| Field | Type | Description |
|-------|------|-------------|
| `modified` | `Option<SystemTime>` | mtime; `None` when the platform gave none |
| `links` | `Option<u32>` | Hardlink count. `Some` on Unix, **`None` on Windows** — the directory listing carries no link count and this project will not open a handle per file to get one (D10) |

#### Candidate

| Field | Type | Description |
|-------|------|-------------|
| `path` | `PathBuf` | What would be removed |
| `category` | `Category` | `RustTarget` / `NodeModules` / `Pycache` / `UserCaches` / `Old` |
| `tier` | `Tier` | `Auto` or `Confirm` |
| `allocated` | `u64` | Attributed bytes — the reclaimable contribution |
| `shared` | `bool` | Contains at least one file with `links > 1`, so `allocated` is an upper bound. Always `false` on Windows, where `links` is unknown — absence of the marker is **not** evidence of unshared content |

#### CleanPlan

| Field | Type | Description |
|-------|------|-------------|
| `candidates` | `Vec<Candidate>` | Sorted by path for determinism |
| `reclaimable` | `u64` | Sum of candidates' `allocated` |
| `excluded` | `Vec<Excluded>` | Path + reason (denylisted, dirty repo) — never silently dropped |

#### CleanOutcome

| Field | Type | Description |
|-------|------|-------------|
| `removed` | `Vec<PathBuf>` | Successfully trashed |
| `failed` | `Vec<TrashFailure>` | Path + reason; the rest still proceeded |
| `reclaimed` | `u64` | Bytes actually freed, i.e. of `removed` only |

### 8.2 Algorithms

#### 8.2.1 Marker-aware junk detection

**Used by:** Task 3

1. Walk the `ScanTree` depth-first from the root.
2. For each node, test each enabled category rule against the node's file name,
   passing the parent's `children` slice so the rule can inspect siblings.
3. `rust-target` matches when the name is `target`, the node is a directory, **and**
   a sibling named `Cargo.toml` exists.
4. `node-modules` matches on the name alone. `pycache` matches `__pycache__`
   directories and `*.pyc` files. `user-caches` matches the three user-scoped cache
   roots only.
5. **On a match, record the node and do not descend into it** — the whole subtree is
   one candidate.
6. On no match, recurse into the node's children.

**Edge cases:**
- `target/` at the scan root with no visible parent: no sibling list, so no marker, so no match. Conservative and correct.
- Nested matches (`node_modules` inside `node_modules`): unreachable, since step 5 stops at the outer one.

#### 8.2.2 Old-file detection

**Used by:** Task 3

0. **If `older_than` is `None`, the rule does not run.** It is opt-in (D9); there is
   no default threshold, and no `old` candidate can exist without the flag.
1. For each node not already claimed by a junk rule, compare `modified` against
   `now - older_than`.
2. Older or exactly equal ⇒ match `old`, confirm-tier.
3. `modified == None` ⇒ never matches. Absence of evidence is not evidence of age.
4. Directories are judged on their own `modified`, not their children's — a
   directory's mtime changes when its entries change, which is the intended signal.

**Why mtime and not atime** (D2): concept §3 offers "atime/mtime", but `relatime`
and `noatime` are widespread mount defaults, and under them atime either lags by a
day or never updates. That makes it least reliable precisely on the cache
directories this rule exists to find — the opposite of what an "unused for N days"
signal needs.

#### 8.2.3 git-dirty guard

**Used by:** Task 5

1. Applies to build-output categories only — `rust-target` in v0.2. Caches and old
   files are unaffected: "mid-work" has no meaning for them.
2. Walk up from the candidate looking for a `.git` entry. No `.git` found ⇒ the
   guard does not apply.
3. `.git` found and no `git` binary on `PATH` ⇒ treat as dirty (conservative).
4. Otherwise run `git status --porcelain` in that repository. Non-empty output ⇒
   dirty ⇒ exclude with reason.
5. `--allow-dirty` skips steps 2–4 entirely.

**Edge cases:**
- `git` present but the repository is corrupt: non-zero exit ⇒ treat as dirty.
- Nested repositories: the nearest `.git` ancestor wins.

### 8.3 Validation Rules

| Rule | Condition | Behaviour | Task |
|------|-----------|-----------|------|
| Denylist | Path is or is under a never-touch root | Never a candidate; no flag overrides | 4 |
| Tilde distinction | `/Library/Caches` vs `~/Library/Caches` | System denied, user-scoped allowed | 3, 4 |
| Virtualenv tier | Name is `venv` or `.venv` | Confirm-tier, never auto | 4 |
| Safe filter | `--safe` set | Confirm-tier candidates excluded | 4 |
| Apply gate | `--apply` absent | Nothing is removed | 6, 7 |
| Age rule off | `older_than == None` | The rule does not run; no `old` candidates | 3, 6 |
| Missing timestamp | `modified == None` | Never matches the age rule | 3 |
| Shared marker | Candidate contains a file with `links > 1` | Marked `shared`; total labelled an upper bound | 4, 6 |
| Purity | Any call to `plan()` | No filesystem write | 4 |

**Never-touch denylist:** `/System`, Windows system directories, `/Library/Caches`
(no tilde), `~/Library/Application Support`, `%APPDATA%`.

**Reclaimable size and hardlinks.** `dedup::attribute` charges a hardlink group's
bytes to its lexicographically first path. Three cases follow, and only one is
wrong:

| Case | Where the keeper is | Total claims | Deleting actually frees | Verdict |
|------|--------------------|--------------|------------------------|---------|
| Every link inside the candidate | inside | the bytes, once | the bytes | **correct** |
| Keeper inside, another link **outside** | inside | the bytes | nothing — the outside link still holds the inode | **over-reports** |
| Keeper outside, a link inside | outside | 0 (the inside node was zeroed) | nothing | **correct** |

So the total is an **upper bound**, tight in the common case and loose only when a
link escapes the candidate set. `links > 1` detects the *possibility* of the middle
case for free, which is what the `shared` marker reports (D7). It deliberately does
not distinguish the first case from the second: doing so needs the full link census
the concept defers, and a conservative "this may be less" is honest where a
confident wrong number would not be.

**Platform note:** `links` is `None` on Windows (D10), so no candidate is ever
marked there. That is a gap in the signal, not a statement about the content — the
report and the README must both say so, or a user comparing macOS and Windows will
draw the wrong conclusion.

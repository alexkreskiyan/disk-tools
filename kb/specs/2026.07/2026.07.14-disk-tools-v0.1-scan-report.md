---
title: "Feature Spec: disk-tools v0.1 — scan + tree report"
description: "Greenfield Rust workspace (core lib + CLI) that walks a path in parallel via read_dir+rayon, measures allocated size, dedups hardlinks deterministically, and prints a dust-style size-sorted tree or JSON."
type: spec
status: draft
version: "1.0.0"
created: 2026-07-14
updated: 2026-07-14
author: skill::write-spec
modified_by: skill::write-spec
concept: "kb/concepts/2026.07/2026.07.14-disk-tools.md"
---

# Feature Spec: disk-tools v0.1 — scan + tree report

## 1. Overview

Build `disk-tools` v0.1: a cross-platform (macOS/Linux/Windows) Rust CLI that scans a directory tree in parallel, measures **real on-disk (allocated) size**, and prints a **dust-style size-sorted tree** of the biggest consumers — or JSON. This is the anchoring capability of a wider disk-utilities suite; cleanup, detectors, config and TUI are later versions.

- **Concept:** [2026.07.14-disk-tools.md](../../concepts/2026.07/2026.07.14-disk-tools.md) (v0.1 row of its Roadmap)
- **Plan:** [2026.07.14-disk-tools-mvp.md](../../plans/2026.07/2026.07.14-disk-tools-mvp.md)
- **Research:** [2026.07.14-technical-rust-disk-cleanup.md](../../research/2026.07/2026.07.14-technical-rust-disk-cleanup.md)

**Codebase state: greenfield.** The repo contains only `.gitignore`, `CLAUDE.md` and `kb/`. No `Cargo.toml`, no `src/`. Everything in this spec is new.

## 2. Approach

### Architecture

```
disk-tools/                          (cargo workspace)
├── core/   disk-tools-core (lib)    ── no logging, no printing, #![forbid(unsafe_code)]
│   ├── lib.rs      pub fn scan(&ScanOptions) -> ScanTree
│   ├── options.rs  ScanOptions
│   ├── walk.rs     read_dir + rayon::scope recursion
│   ├── size.rs     allocated (filesize) + apparent
│   ├── dedup.rs    (dev,inode) / (volume,file_index) → deterministic attribution
│   ├── tree.rs     ScanNode / ScanTree, bottom-up aggregation
│   └── units.rs    size formatting
└── cli/    disk-tools (bin)
    ├── main.rs     args → ScanOptions → core::scan → render
    ├── args.rs     clap derive
    └── render/{tree.rs, json.rs}

flow:  args → ScanOptions → core::scan ─┬─ walk   (parallel, per-directory)
                                        ├─ size   (allocated + apparent)
                                        ├─ dedup  (resolve attribution)
                                        └─ aggregate ⇒ ScanTree { root, skipped }
                                                          ↓
                          CLI: tree-render (bars) │ json    + skipped summary
```

### Design Decisions

| Decision | Chosen | Rationale | Alternatives |
|----------|--------|-----------|--------------|
| Walker | **`std::fs::read_dir` + `rayon::scope` recursion** | True diskus-style. `walkdir` is a *sequential recursive* iterator — it cannot sit inside a parallel recursion (only `max_depth(1)`, i.e. `read_dir` with overhead). Since symlinks are not followed, loop detection is a non-issue; `same_file_system` is a simple `st_dev` check. **`walkdir` drops from dependencies.** | `walkdir` sequential + `rayon` par_iter on sizing (parallelizes the real bottleneck, keeps error-context free); `jwalk` (stale); `ignore` (gitignore hides the artifacts we hunt) |
| Hardlink attribution | **Lexicographically-first path**, resolved in a post-collection pass | "First occurrence" is *nondeterministic under a parallel walk* — dir totals would drift run-to-run and tests would flake. Lex-first is reproducible and still sums to the root total. | First-walked (nondeterministic); dedup only the root total (dir totals then double-count) |
| `--depth` semantics | **Display depth; traversal always full** | Matches real `du --max-depth`, which limits *output*, not counting. Limiting traversal would make directory sizes partial and misleading. | Traversal limit (faster, but sizes lie — would need an explicit "incomplete" marker) |
| `--min-size` semantics | **Display filter** | Same trap as `--depth`: filtering during aggregation would corrupt parent totals. | Aggregation filter (wrong totals) |
| Phase order | collect → resolve dedup → aggregate | Attribution must settle before any sums are computed | Dedup during walk (nondeterministic, see above) |
| serde in core | Behind a `serde` feature flag | Don't force serde on library consumers; the CLI enables it for `--json` | Always-on serde; CLI-side DTO mapping |
| Terminal width | `terminal_size` crate, fixed fallback | Bars must degrade gracefully when piped (non-tty) | Hardcoded width (breaks in wide/narrow terminals) |
| JSON purity | JSON → **stdout**; spinner + skipped summary → **stderr** | Keeps `--json` output machine-parseable when piped | Everything to stdout (corrupts JSON) |
| Progress | `indicatif` **spinner** (unbounded) | Total file count is unknown before the walk finishes | Progress bar (needs a total) |

### Components

**New (all of them):**
- `disk-tools-core` — walk, size, dedup, aggregation, model, units
- `disk-tools` — clap CLI, tree renderer, JSON renderer, progress/skipped presentation
- `.github/workflows/ci.yml`, `README.md`

**Modified:** none (greenfield).

## 3. Tasks

### Task 1: Workspace scaffold + model types

- **Goal:** Compiling workspace with `core` lib + `cli` bin; model and options types defined.
- **Acceptance Criteria:**
  - [x] Given the repo, when `cargo build --workspace` runs, then both crates compile
  - [x] Given `ScanOptions::default()`, when inspected, then `min_size == 0`, `apparent == false`, `one_file_system == false`, `depth == None`
  - [x] Given `core`, when compiled, then `#![forbid(unsafe_code)]` is in effect (build fails if `unsafe` is introduced)
  - [x] Given the workspace manifest, when inspected, then MSRV is pinned (`rust-version`, ≥ 1.85 for `rayon`) and edition 2024 *(revised during Task 1: edition 2024 stabilized in exactly 1.85, so the spec's own MSRV makes it free — was 2021)*
  - [x] Given the model, when constructed, then `ScanNode` / `ScanTree` / `SkippedEntry` expose the fields in §8.1
- **Tests first (TDD):**
  - [x] `options_default_values`
  - [x] `scan_tree_construction` (field shape)
- **Key files:** `Cargo.toml`, `core/Cargo.toml`, `core/src/{lib,options,tree}.rs`, `cli/Cargo.toml`, `cli/src/main.rs`
- **Dependencies:** None
- **Status:** Done
- **Handoff:** [2026.07.14-disk-tools-v0.1-scan-report-task-1.md](../../handoffs/2026.07/2026.07.14-disk-tools-v0.1-scan-report-task-1.md)

---

### Task 2: Sizing — allocated + apparent

- **Goal:** Cross-platform per-file size measurement.
- **Acceptance Criteria:**
  - [ ] Given a file with known content length, when measured, then `apparent` equals that length
  - [ ] Given a sparse file (`#[cfg(unix)]`), when measured, then `allocated < apparent`
  - [ ] Given a normal small file, when measured, then `allocated` is a whole number of filesystem blocks
- **Tests first (TDD):**
  - [ ] `apparent_equals_content_length`
  - [ ] `sparse_file_allocated_less_than_apparent` (`#[cfg(unix)]`)
  - [ ] `allocated_is_block_multiple`
- **Key files:** `core/src/size.rs`
- **Dependencies:** Requires Task 1
- **Status:** Pending

#### Implementation Notes
- Use the `filesize` crate (Unix `blocks()*512`, Windows `GetCompressedFileSizeW`). See §8.2.1.
- Use `DirEntry::file_type()` for dir/file/symlink decisions — **never** `metadata()` speculatively; the single metadata call happens once per real file, where size is needed anyway.

---

### Task 3: Parallel walk + error collection

- **Goal:** `read_dir` + `rayon::scope` recursion producing raw entries, collecting skips as data.
- **Acceptance Criteria:**
  - [ ] Given a nested fixture tree, when walked, then every regular file is visited exactly once
  - [ ] Given an unreadable directory, when walked, then it appears in `skipped` with a permission reason and the walk completes normally (no panic, no abort)
  - [ ] Given a symlink to a directory, when walked with defaults, then it is **not** followed
  - [ ] Given a path that vanishes between `read_dir` and `stat` (TOCTOU), when walked, then it is recorded in `skipped` with a not-found reason, not fatal
  - [ ] Given a mount boundary and `one_file_system = true`, when walked, then traversal does not cross it *(test `#[ignore]` — needs a real mount point; run manually)*
  - [ ] Given a path exceeding Windows `MAX_PATH` (`#[cfg(windows)]`), when walked, then it is handled via the `\\?\` prefix fallback and not skipped as an error; `canonicalize()` is never called
- **Tests first (TDD):**
  - [ ] `visits_each_file_once`
  - [ ] `unreadable_dir_collected_as_skipped` (`#[cfg(unix)]`, chmod 000)
  - [ ] `symlink_not_followed_by_default` (`#[cfg(unix)]`)
  - [ ] `vanished_file_recorded_as_skipped`
  - [ ] `one_file_system_stops_at_boundary` (`#[ignore]`)
  - [ ] `long_path_handled` (`#[cfg(windows)]`)
- **Key files:** `core/src/walk.rs`
- **Dependencies:** Requires Task 1, Task 2
- **Status:** Pending

#### Implementation Notes
- Algorithm: §8.2.2. Own `st_dev` check for `one_file_system` — no `walkdir`.
- Symlinks are not followed → no loop detection needed on the default path.

---

### Task 4: Hardlink dedup with deterministic attribution

- **Goal:** Each inode's bytes counted once, reproducibly.
- **Acceptance Criteria:**
  - [ ] Given two hardlinks to one inode in different directories, when scanned, then the bytes are counted exactly once in the root total
  - [ ] Given the same tree, when scanned, then the bytes are attributed to the **lexicographically first** of the linked paths
  - [ ] Given the same tree, when scanned twice, then all directory totals are byte-identical across runs
- **Tests first (TDD):**
  - [ ] `hardlink_counted_once` (`#[cfg(unix)]`)
  - [ ] `hardlink_attributed_to_lexicographically_first_path` (`#[cfg(unix)]`)
  - [ ] `dir_totals_deterministic_across_runs`
- **Key files:** `core/src/dedup.rs`
- **Dependencies:** Requires Task 3
- **Status:** Pending

#### Implementation Notes
- Key: `(st_dev, st_ino)` on Unix; `(volume_serial_number(), file_index())` on Windows — both available on `std` `MetadataExt`. See §8.2.3.
- **Do not** trust Windows `number_of_links()` (documented as unreliable); dedup by identity pair only.

---

### Task 5: Bottom-up aggregation → ScanTree

- **Goal:** Size-annotated tree with correct subtree totals.
- **Acceptance Criteria:**
  - [ ] Given a fixture tree of known sizes, when aggregated, then each directory total equals the sum of its children's post-dedup sizes
  - [ ] Given a fixture tree, when scanned, then the root total matches `du -sh` within block-rounding (`#[cfg(unix)]`)
  - [ ] Given a tree containing a cross-directory hardlink, when aggregated, then the sum of directory totals equals the root total (no double count)
- **Tests first (TDD):**
  - [ ] `dir_total_equals_sum_of_children`
  - [ ] `root_total_matches_du` (`#[cfg(unix)]`, integration)
  - [ ] `dir_totals_sum_to_root_with_hardlinks`
- **Key files:** `core/src/tree.rs`
- **Dependencies:** Requires Task 3, Task 4
- **Status:** Pending

---

### Task 6: CLI args → ScanOptions

- **Goal:** clap derive frontend producing `ScanOptions`.
- **Acceptance Criteria:**
  - [ ] Given no arguments, when run, then it exits non-zero with a usage error and **does not scan the CWD**
  - [ ] Given `<PATH>`, when parsed, then `ScanOptions.root` is that path
  - [ ] Given `--min-size 1M`, when parsed, then it becomes 1048576 bytes
  - [ ] Given `--help`, when run, then all six flags are listed (`-n`, `--min-size`, `--depth`, `--apparent`, `--one-file-system`, `--json`)
  - [ ] Given a nonexistent `<PATH>`, when run, then it exits non-zero with a clear error before scanning
- **Tests first (TDD):**
  - [ ] `missing_path_errors_and_does_not_scan_cwd`
  - [ ] `path_maps_to_scan_options_root`
  - [ ] `min_size_suffix_parsing`
  - [ ] `help_lists_all_flags`
  - [ ] `nonexistent_path_errors_early`
- **Key files:** `cli/src/args.rs`, `cli/src/main.rs`
- **Dependencies:** Requires Task 1
- **Status:** Pending

---

### Task 7: Tree renderer (dust-style bars, `-n` / `--depth` / `--min-size`)

- **Goal:** The default human report.
- **Acceptance Criteria:**
  - [ ] Given a tree and `-n K`, when rendered, then at most K entries are printed
  - [ ] Given `--depth D`, when rendered, then no entry deeper than D is shown, **and** shown directory totals still reflect their full subtree (du semantics)
  - [ ] Given `--min-size S`, when rendered, then entries below S are hidden **and** parent totals are unchanged
  - [ ] Given sibling entries, when rendered, then they are sorted by size descending with a bar proportional to the largest sibling
  - [ ] Given `--apparent`, when rendered, then ranking and sizes use apparent, not allocated
  - [ ] Given non-tty stdout (piped), when rendered, then a fixed fallback width is used and no panic occurs
- **Tests first (TDD):**
  - [ ] `n_limits_entries_shown`
  - [ ] `depth_limits_display_not_totals`
  - [ ] `min_size_hides_entries_without_changing_totals`
  - [ ] `siblings_sorted_desc_with_proportional_bars`
  - [ ] `apparent_flag_switches_ranking_unit`
  - [ ] `non_tty_uses_fallback_width`
- **Key files:** `cli/src/render/tree.rs`
- **Dependencies:** Requires Task 5, Task 6
- **Status:** Pending

---

### Task 8: JSON output

- **Goal:** Machine-readable output.
- **Acceptance Criteria:**
  - [ ] Given a scan, when `--json` is passed, then stdout is valid JSON that parses back
  - [ ] Given the same scan, when compared, then JSON sizes/entries match the human report's numbers
  - [ ] Given skipped entries, when `--json` is passed, then they appear in the JSON payload
- **Tests first (TDD):**
  - [ ] `json_output_parses`
  - [ ] `json_matches_human_report_numbers`
  - [ ] `json_includes_skipped`
- **Key files:** `cli/src/render/json.rs`, `core` `serde` feature
- **Dependencies:** Requires Task 5, Task 6
- **Status:** Pending

---

### Task 9: Progress spinner + skipped summary

- **Goal:** Presentation of progress and skips, without corrupting machine output.
- **Acceptance Criteria:**
  - [ ] Given a tty, when scanning, then a spinner is shown and cleared on completion
  - [ ] Given N skipped paths, when the scan ends, then a count plus the first 10 paths are shown
  - [ ] Given `--verbose`, when the scan ends, then all skipped paths are listed
  - [ ] Given `--json`, when run, then the spinner and skipped summary go to **stderr** and stdout stays valid JSON
- **Tests first (TDD):**
  - [ ] `skipped_summary_caps_at_ten_with_count`
  - [ ] `verbose_lists_all_skipped`
  - [ ] `json_stdout_stays_clean_progress_to_stderr`
- **Key files:** `cli/src/main.rs`
- **Dependencies:** Requires Task 7
- **Status:** Pending

---

### Task 10: Cross-platform CI + README

- **Goal:** Green on all three targets, documented.
- **Acceptance Criteria:**
  - [ ] Given a push, when CI runs, then `just verify` (fmt-check + lint + test) plus `just build` pass on macOS, Linux and Windows — CI calls the justfile recipes rather than re-listing the raw cargo commands
  - [ ] Given the Windows job, when tests run, then `#[cfg(unix)]`-gated symlink tests are skipped and the job is green without privilege setup
  - [ ] Given the README, when read, then it documents usage, all flags, and the **APFS COW clone overcount** limitation
- **Key files:** `.github/workflows/ci.yml`, `README.md`
- **Dependencies:** Requires Tasks 1–9
- **Status:** Pending

---

### Task 11: Performance + memory validation

- **Goal:** Prove the "fast" claim and observe the known memory trade-off.
- **Acceptance Criteria:**
  - [ ] Given a `node_modules`-heavy tree, a cache directory and a flat media library, when benchmarked against `du -sh` and `diskus`, then wall-clock numbers are recorded in the repo
  - [ ] Given a warm cache, when scanning, then `disk-tools` is not slower than `du -sh`
  - [ ] Given a large scan (≥1M files), when measured, then peak RSS is recorded, quantifying the full-tree-in-memory trade-off
- **Key files:** `README.md` or `kb/` benchmark note
- **Dependencies:** Requires Task 7
- **Status:** Pending

## 4. Boundaries

### In Scope
- Parallel scan, allocated + apparent sizing, hardlink dedup, bottom-up aggregation
- dust-style tree report, JSON output, spinner, skipped summary
- Cross-platform CI, README, perf/memory validation

### Out of Scope (later versions per concept Roadmap)
- Detectors (junk-patterns, old files, duplicates) — v0.2 / v0.5
- Cleanup engine, trash, safe-list, git-dirty guard — v0.2
- TOML config, `--config` — v0.3
- Interactive TUI — v0.4
- nlink-aware "reclaimable size" — deferred with cleanup

### Protected Elements (DO NOT CHANGE)
- **`disk-tools-core` never logs or prints.** No `log`/`tracing`/`println!` in the lib; skips are returned as data. Presentation is the CLI's job.
- **`#![forbid(unsafe_code)]` in core.**
- **Core knows nothing about TOML, config paths, or terminals.**

### Constraints
- Rank by allocated size by default; apparent only via `--apparent`
- `<PATH>` always required — never scan CWD implicitly
- Never call `canonicalize()` (Windows `\\?\` interop breakage); walk raw paths
- Full tree retained in memory (required for dir totals + future TUI) — accepted trade-off
- APFS COW clones overcount shared blocks — documented, not fixed

## 5. Codebase Context

### Project Structure
Greenfield. Target layout is the diagram in §2. Existing tracked files: `.gitignore` (already covers `/target/`), `CLAUDE.md`, `kb/`.

### Conventions
- From `CLAUDE.md`: all code changes must be reflected in docs; refresh `kb/` snapshots via `/document-repository` after significant changes.
- KB layout: `kb/<folder>/<YYYY.MM>/<YYYY.MM.DD>-<slug>.md`.
- Naming/idiom: standard Rust — `snake_case` modules, `cargo fmt` defaults, clippy clean.

### Build & Test Commands

`justfile` at the repo root is the single entry point; raw cargo shown for reference.

| Recipe | Runs |
|--------|------|
| `just build` | `cargo build --workspace` |
| `just test` | `cargo test --workspace` |
| `just lint` | `cargo clippy --workspace --all-targets -- -D warnings` |
| `just fmt` / `just fmt-check` | `cargo fmt --all` / `cargo fmt --all -- --check` |
| `just verify` | `fmt-check` + `lint` + `test` — the pre-commit gate |
| `just run <ARGS>` | `cargo run -p disk-tools -- <ARGS>` |

## 6. Testing Strategy

**TDD workflow:** each task is implemented test-first — write a failing test for an acceptance criterion (red), make it pass simply (green), refactor. No production code without a failing test driving it.

### Acceptance Criteria → Tests

| Task | Criterion (abbrev.) | Test | Type |
|------|--------------------|------|------|
| 1 | defaults correct | `options_default_values` | unit |
| 2 | apparent == length | `apparent_equals_content_length` | unit |
| 2 | sparse: allocated < apparent | `sparse_file_allocated_less_than_apparent` | unit (unix) |
| 3 | each file visited once | `visits_each_file_once` | unit |
| 3 | unreadable dir → skipped | `unreadable_dir_collected_as_skipped` | unit (unix) |
| 3 | symlink not followed | `symlink_not_followed_by_default` | unit (unix) |
| 3 | vanished file → skipped | `vanished_file_recorded_as_skipped` | unit |
| 3 | one-file-system | `one_file_system_stops_at_boundary` | integration (`#[ignore]`) |
| 3 | Windows long path | `long_path_handled` | unit (windows) |
| 4 | hardlink counted once | `hardlink_counted_once` | unit (unix) |
| 4 | lex-first attribution | `hardlink_attributed_to_lexicographically_first_path` | unit (unix) |
| 4 | determinism | `dir_totals_deterministic_across_runs` | unit |
| 5 | dir total == Σ children | `dir_total_equals_sum_of_children` | unit |
| 5 | root == `du -sh` | `root_total_matches_du` | integration (unix) |
| 6 | no path → error, no CWD scan | `missing_path_errors_and_does_not_scan_cwd` | integration |
| 7 | `-n` caps entries | `n_limits_entries_shown` | unit |
| 7 | `--depth` display-only | `depth_limits_display_not_totals` | unit |
| 7 | `--min-size` display-only | `min_size_hides_entries_without_changing_totals` | unit |
| 7 | `--apparent` ranking | `apparent_flag_switches_ranking_unit` | unit |
| 8 | JSON parses / matches | `json_output_parses`, `json_matches_human_report_numbers` | integration |
| 9 | JSON stdout purity | `json_stdout_stays_clean_progress_to_stderr` | integration |
| 10 | CI green ×3 | CI matrix | ci |

### Test Patterns
No existing patterns (greenfield). Establish:
- Unit tests in-module (`#[cfg(test)] mod tests`), integration tests in `tests/`.
- Fixture trees built in a temp dir (`tempfile` dev-dependency) with known byte sizes.
- Platform gating: `#[cfg(unix)]` for symlink/hardlink/permission/sparse fixtures; `#[cfg(windows)]` for long-path. **Windows symlink tests are deliberately skipped** (need admin/Developer Mode) to keep CI green.
- Tests needing a real mount point are `#[ignore]` and run manually.

## 7. Open Questions

Must be empty before implementation starts.

**None — empty. Implementation may start.**

*Resolved 2026-07-14:* the concept-drift item (walker `read_dir`+`rayon::scope` with `walkdir` dropped; `--depth` as a display limit; lexicographically-first hardlink attribution; plus `--min-size` as a display filter, `terminal_size` dependency, `--verbose` flag, and JSON/stderr separation) was synced back into [the concept](../../concepts/2026.07/2026.07.14-disk-tools.md) and [the plan](../../plans/2026.07/2026.07.14-disk-tools-mvp.md). All three documents now agree.

## 8. Implementation Reference

### 8.1 Data Structures

#### ScanOptions

| Field | Type | Description |
|-------|------|-------------|
| `root` | `PathBuf` | Scan root (always explicit) |
| `min_size` | `u64` | Display threshold in bytes (default 0) |
| `depth` | `Option<usize>` | Display depth (default `None` = unlimited) |
| `apparent` | `bool` | Rank by apparent instead of allocated |
| `one_file_system` | `bool` | Don't cross `st_dev` boundaries |

#### ScanNode

| Field | Type | Description |
|-------|------|-------------|
| `path` | `PathBuf` | Absolute or root-relative path |
| `allocated` | `u64` | On-disk bytes (post-dedup attribution) |
| `apparent` | `u64` | Logical bytes |
| `is_dir` | `bool` | Directory vs file |
| `children` | `Vec<ScanNode>` | Empty for files |

#### ScanTree

| Field | Type | Description |
|-------|------|-------------|
| `root` | `ScanNode` | Aggregated tree |
| `skipped` | `Vec<SkippedEntry>` | Collected, never logged by core |

#### SkippedEntry

| Field | Type | Description |
|-------|------|-------------|
| `path` | `PathBuf` | Entry that could not be processed |
| `reason` | `SkipReason` | `PermissionDenied` \| `NotFound` \| `Other(String)` |

### 8.2 Algorithms

#### 8.2.1 Per-file sizing
**Used by:** Task 2
1. From `DirEntry::file_type()` decide dir / file / symlink — **no** `metadata()` for this.
2. For a regular file, take metadata **once**.
3. `apparent = metadata.len()`.
4. `allocated = filesize::file_real_size_fast(path, &metadata)` (Unix `blocks()*512`; Windows `GetCompressedFileSizeW`).

**Edge cases:** sparse → `allocated < apparent`; compressed (NTFS/APFS) → `allocated < apparent`; metadata fails → `SkippedEntry`.

#### 8.2.2 Parallel walk
**Used by:** Task 3
1. `read_dir(dir)`; on error → push `SkippedEntry`, return.
2. For each entry: `file_type()`.
3. Symlink → record as a (tiny) entry, do **not** follow.
4. Directory → if `one_file_system` and its `st_dev` differs from the root's, skip; else `rayon::scope`-spawn recursion.
5. File → size it (§8.2.1), record `(path, allocated, apparent, identity_key)`.
6. Join children; return raw entries upward.

**Edge cases:** permission denied → skipped, continue; vanished mid-walk (`NotFound`) → skipped, continue; Windows `MAX_PATH` → retry with `\\?\` prefix; **never** `canonicalize()`.

#### 8.2.3 Deterministic hardlink attribution
**Used by:** Task 4
1. During the walk, record each file's identity key: `(st_dev, st_ino)` (Unix) / `(volume_serial_number, file_index)` (Windows).
2. After collection, group entries by identity key.
3. For each group with >1 path, sort paths lexicographically; attribute full bytes to the **first**, zero to the rest.
4. Aggregate (§8.2.4) using attributed sizes.

**Edge cases:** links spanning directories → dir totals still sum to root; ReFS 64-bit file IDs are not unique (out of scope); Windows `number_of_links()` is unreliable — not used.

#### 8.2.4 Bottom-up aggregation
**Used by:** Task 5
1. Build the node tree from attributed entries.
2. Post-order: `dir.allocated = Σ children.allocated`; same for apparent.
3. Do **not** apply `min_size` / `depth` here — they are display-time filters (§8.2.5).

#### 8.2.5 Tree rendering
**Used by:** Task 7
1. Choose the ranking unit: `apparent` ? apparent : allocated.
2. Sort each node's children by that unit, descending.
3. Walk the tree emitting entries until `n` is exhausted, pruning branches deeper than `depth` and entries below `min_size` — **totals shown are the full-subtree totals**, unaffected by these filters.
4. Bar width ∝ entry size / largest-sibling size, scaled to terminal width (`terminal_size`, fixed fallback when non-tty).

### 8.3 Validation Rules

| Rule | Condition | Behavior | Task |
|------|-----------|----------|------|
| Path required | no positional arg | usage error, non-zero exit, no scan | 6 |
| Path exists | `<PATH>` missing on disk | clear error before scanning | 6 |
| Size suffix | `--min-size 1M` | parse to 1048576 | 6 |
| Unreadable dir | `EACCES` | `SkippedEntry`, continue | 3 |
| Vanished entry | `ENOENT` mid-walk | `SkippedEntry`, continue | 3 |
